import { CustomRouteSerializer } from "./custom-route.serializer";

describe("CustomRouteSerializer", () => {
  it("should create an instance", () => {
    expect(new CustomRouteSerializer()).toBeTruthy();
  });
});
